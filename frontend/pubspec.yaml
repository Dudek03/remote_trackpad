name: remote_trackpad_frontend
description: Remote Trackpad client for Android and iOS.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  shared_preferences: ^2.0.0
  wakelock_plus: ^1.0.0

flutter:
  uses-material-design: true
